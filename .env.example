# 실제 값을 채운 .env 파일을 만들어서 서버에만 두세요 (git에 커밋하지 마세요 — .gitignore에 이미 추가됨).
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# 이 브랜치는 네이버 로그인도 같이 등록되어 있어서, 값이 없으면 앱이 아예 기동을 못 합니다.
# 네이버 로그인을 안 쓸 거면 아무 문자열이나 넣어도 됩니다 (실제 네이버 버튼만 누르지 않으면 문제없음).
NAVER_CLIENT_ID=
NAVER_CLIENT_SECRET=

# 배포한 sslip.io 주소. 예: https://3-35-12-34.sslip.io
# 구글 로그인 성공 후 리다이렉트할 프론트 주소
APP_FRONTEND_BASE_URL=
# 그룹 초대 링크를 만들 때 붙는 주소 (보통 위와 동일한 값)
APP_PUBLIC_BASE_URL=
